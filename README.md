# Aplikasi Cuti Karyawan Booble.id


# Installation

```
# Install Depedency
$ composer install
$ npm install

# Setup Environment Variable
$ cp .env.example .env
$ php artisan key:generate

# Migrate & Seed
$ php artisan migrate --seed

```

